function graphFactory (verticesCount) {
    adjList = (()=> {
        let temp = [];
        for (let i = 0; i < verticesCount; i++)
        {
            temp.push([])
        }
        return list;
    })();
    const graph = {
        
    };
    return graph;
}

module.exports = graphFactory;