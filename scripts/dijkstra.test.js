const dijkstra = require('./dijkstra.js');

describe("canary", ()=> {
    it("test testing env", () => {
        expect(true).toBe(true);
    })
});

describe ("dijkstra", ()=> {
    const graph = [{0:[0]}]
    it("dijkstra should", ()=> {
        expect(true).toBe(true);
    })
})